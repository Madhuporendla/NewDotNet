apiVersion: apps/v1
kind: Deployment
metadata:
  name: madhupdocker-app-deployment
  namespace: madhu
  labels:
    app: madhupdocker-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: madhupdocker-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        app: madhupdocker-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: envi
                    operator: In
                    values:
                      - prod
      containers:
        - name: madhupdocker-app
          image: madhuporendla/madhupdocker-app:latest
          ports:
            - containerPort: 5000
          livenessProbe:
            httpGet:
              path: /_status/healthz
              port: 5000
            initialDelaySeconds: 8
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /readiness
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 5
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: madhupdocker-app-deployment
                  key: DB_HOST