apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: madhupdocker-app-hpa
  namespace: madhu
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: madhudocker-app-deployment
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50